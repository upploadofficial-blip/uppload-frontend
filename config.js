// config.js — shared by every page.
// This is the ONLY place you need to change the backend address.
//
// While testing on your own computer, leave it as localhost.
// Once your backend is deployed on Render, change this one line to your
// live Render URL (e.g. "https://uppload-backend.onrender.com") and every
// page in this frontend will automatically use it.

const CONFIG = {
  API_BASE: 'https://uppload-backend.onrender.com'
};
